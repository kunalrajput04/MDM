// filepath: src/app/shared/utility.ts
export class Utility {
    // Add methods or properties as needed
    formatDate(date: Date): string {
      return date.toISOString(); // Example utility method
    }
  }