export interface AgridRequest {
  length: number;
  searchColumn: string;
  searchValue: string;
  pageNumber: number;
  accessLevel: string;
  accessValue: number;
}

export interface AgridRequests {
  length:number,
  searchColumn:string,
  searchValue:string,
  pageNumber:number
}